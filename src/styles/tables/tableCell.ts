import { CSSProperties } from "react";

export const minWidth = 140;
export const maxWidth = 200;

export const border = "1px solid #7c7c7c";

export const CELL_VALUE = {
  border,
  textAlign: "right",
} as CSSProperties;
